<% @staging.each do |staging| %>
  <% if notice %>
    <p class="alert alert-success alert-dismissable">
       <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-warning alert-dismissable"><button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button><%= alert %></p>
  <% end %>

  <% if on_my_page? %>
    <p>
      <b id="new_container_label">New Container</b>
        <%= link_to '', new_user_container_path(@user) , class:"glyphicon glyphicon-plus", id: "new_container_plus", remote: true %>
    </p>
    <p>
      <b id="new_link_label">New Link</b>
        <%= link_to "", new_container_link_path(staging), class:"glyphicon glyphicon-plus", id: "stagingnew", remote: true %>
    </p>
    <a class="exclude" id="staging-link" href="javascript:void(0);">View Staging <span class="glyphicon glyphicon-new-window"></span></a>
    <a class="exclude" id="staging-link-2" href="javascript:void(0);">Hide Staging <span class="glyphicon glyphicon-remove"></span></a>


  <% end%>
  <hr class="exclude1" id="welcome-page-hr">
  <table class="staging-left" id="reveal-this">
    <tbody>
      <% staging.links.each do |link| %>
        <tr>
          <td colspan="2">
            <%= link_to link.name, url_with_protocol(link.url), :target => '_blank' %>
              <% if on_my_page? %>
                <%= link_to '', [staging, link], method: :delete, data: { confirm: 'Are you sure?' }, class:"glyphicon glyphicon-trash container-icon", id: "staging_trash_link" %>
                <%= link_to '', edit_container_link_path(staging, link), class: "glyphicon glyphicon-edit container-icon", id: "link_edit", remote: true %>
              <% end %>
          </td>
        </tr>
        <tr>
          <td> &nbsp&nbsp
             <%= link.description %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

