
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->

  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">Link Sensei</a>
  </div> <!-- button will only appear when browser is minimized to col-sm-#  -->

  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-right">
      <li><% if current_user.present? %> <%= current_user.email %> <% end %></li>
      <li class="<%= 'active' if current_page?(root_path)%>">
        <%= link_to "Home", root_path, class:"nav-link" %>
      </li>
      <li class="<%= 'active' if current_page?(destroy_user_session_path)%>">
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete , class:"nav-link"%>
      </li>
    </ul>
  </div>  <!--end of navigational headings -->
</nav>

<div class="container" >
  <div class="col-lg-3 col-lg-offset-1">

<p><%= link_to 'New Container', new_user_container_path(@user) %></p>

<h1>Containers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @user.containers.each do |container| %>
      <tr>
        <td><%= container.name %></td>
        <td><%= container.description %></td>
        <td><%= link_to 'Show', user_container_path(@user, container) %>
        <%= link_to 'Edit', edit_user_container_path(@user, container) %>
        <%= link_to 'Destroy', [@user, container], method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

  </div>
  <div class="col-lg-7"
</div>
